# VisionRemoteAI

An AI-powered control system that uses visual understanding to interact with an operating device through controlled mouse, keyboard, scrolling, and gesture actions. The AI observes the screen, decides what action to take, executes it, then observes the updated screen and continues until the requested task is completed.
